import React, { useState } from "react";
import { ShowValueCheckbox } from "./ShowValueCheckbox.jsx";
import { Input } from "./Input.jsx";
import { RadioButtons } from "./RadioButtons.jsx";
import styles from "../ProgressBarConfiguration.module.scss";

export const LabelBlock = ({
    propertyKey,
    label,
    colorIndex,
    radioPropertyName,
}) => {
    return (
        <div className={styles.input_row}>
            <ShowValueCheckbox propertyKey={propertyKey} label={label} />
            <Input propertyName={"color"} index={colorIndex} />
            <RadioButtons propertyName={radioPropertyName} />
        </div>
    );
};
