import React, { useState } from "react";
import { Input } from "./Input.jsx";

export const IndicatorInputBlock = ({
    propertyName,
    blockName,
    blockClass,
}) => {
    return (
        <div className={blockClass}>
            <span>{blockName}</span>
            <Input propertyName={propertyName} index={0} />
            <Input propertyName={propertyName} index={1} />
        </div>
    );
};
